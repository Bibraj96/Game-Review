<h1>Add a New Game to Review!</h1>

<%= form_for(@game) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <p> Choose an existing genre for this game: </p>
  <%= f.collection_select :genre_id, Genre.all, :id, :name%>

  <p> Create a new brand for this game: </p>
  <%= f.fields_for :genre 

  <%= f.submit %>
<% end %>