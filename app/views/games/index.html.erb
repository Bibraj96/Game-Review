<h1>All Games</h1>

<ul>
<% @games.each do |game| %>
  <li><strong><%= link_to game.title, game_path(game) %></strong></li>
  Genre: <%= game.genre.name %><br>
  <%= link_to "Review this game!", new_game_review_path(game) %>
<% end %>
</ul>