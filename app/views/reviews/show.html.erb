<h1><%= @review.game.title %> Review</h1>
<h2>Score: <%= @review.rating %>/10</h2>
<p><%= @review.content %></p>
<p>Written By: <%= @review.user.username %></p>
<div class="actions">
  <%= link_to "Edit Review", edit_review_path(@review), class: "btn btn-primary" if @review.user == current_user%><br><br>
  <%= button_to 'Delete Review', review_path(@review), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-primary" if @review.user == current_user %>
</div>